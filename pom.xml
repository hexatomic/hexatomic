<project>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>org.corpus_tools.hexatomic.root</artifactId>
    <packaging>pom</packaging>

    <!-- PARENT -->

    <parent>
        <groupId>org.corpus_tools</groupId>
        <artifactId>org.corpus_tools.hexatomic.configuration</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>./releng/org.corpus_tools.hexatomic.configuration</relativePath>
    </parent>

    <!-- MODULES -->

    <modules>
        <module>bundles</module>
        <module>features</module>
        <module>releng</module>
    </modules>

    <!-- BUILD -->
    <build>
        <plugins>
            <plugin>
                <groupId>com.amashchenko.maven.plugin</groupId>
                <artifactId>gitflow-maven-plugin</artifactId>
                <version>1.12.0</version>
                <configuration>
                    <tychoBuild>true</tychoBuild>
                    <gpgSignTag>true</gpgSignTag>
                    <gpgSignCommit>true</gpgSignCommit>
                    <verbose>true</verbose>
                    <skipFeatureVersion>true</skipFeatureVersion>
                    <versionDigitToIncrement>1</versionDigitToIncrement>
                    <pushRemote>true</pushRemote>
                    <keepRemote>false</keepRemote>
                    <gitFlowConfig>
                        <versionTagPrefix>v</versionTagPrefix>
                    </gitFlowConfig>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>